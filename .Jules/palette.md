## 2026-06-09 - Accessibility Checker Insights
**Learning:** The `accessibility_checker.py` script specifically flags missing `lang` attributes, lack of `aria-labels` on inputs/buttons, missing skip-to-content links, and click handlers without keyboard support (`onKeyDown`).
**Action:** These are prime targets for Palette's daily UX enhancements. Small, impactful changes that improve accessibility for all users.
