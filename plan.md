1. Add `allSelected` and `someSelected` to `InventoryMobileGroupRow` props in `src/components/InventoryRows.tsx` to match the updated `GroupRowProps` definition, even though it doesn't currently render checkboxes. This satisfies the TypeScript interface mapping and ensures consistency.

2. Verify compilation after modifying `InventoryMobileGroupRow`.

3. Re-run code review.
